cargo-features = ["strip"]

[workspace]
members = [
    "clashctl",
    "clashctl-bin",
    "clashctl-tui",
    "clashctl-cli",
    "clashctl-interactive",
    "clashctl-workspace-hack",
]

[profile.release]
lto = true
strip = true
panic = "abort"
opt-level = "z"
codegen-units = 1
